Внеси правки в комментарий по patch_plan.

Правила:
- Не переписывать >35% текста.
- Не добавлять фактов, которых нет во входных данных.
- Соблюдать constraints автора.
- Пунктуация: только точка и запятая. Удалить длинное тире (—) и двоеточие (:).
- Вернуть только исправленный текст комментария, без JSON, без пояснений.

patch_plan:
{patch_plan}

Ограничения автора:
{constraints}

Текущий черновик:
{draft}

Верни только исправленный текст комментария.
